import { MenuButtonsDirective } from './menu-buttons.directive';

describe('MenuButtonsDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuButtonsDirective();
    expect(directive).toBeTruthy();
  });
});
